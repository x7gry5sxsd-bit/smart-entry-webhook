services:
  - type: web
    name: smart-entry-webhook
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn webhook_service:app --host 0.0.0.0 --port $PORT
    plan: free
    
